apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc-users
spec:
  replicas: 2
  selector:
    matchLabels:
      app: svc-users
  template:
    metadata:
      labels:
        app: svc-users
    spec:
      containers:
        - name: svc-users
          image: ghcr.io/ORG/svc-users:TAG
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet: { path: /health, port: 8000 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 8000 }
            initialDelaySeconds: 10
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: svc-users
spec:
  selector:
    app: svc-users
  ports:
    - port: 80
      targetPort: 8000
  type: ClusterIP
