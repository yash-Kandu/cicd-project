apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc-notifications
spec:
  replicas: 2
  selector:
    matchLabels:
      app: svc-notifications
  template:
    metadata:
      labels:
        app: svc-notifications
    spec:
      containers:
        - name: svc-notifications
          image: ghcr.io/ORG/svc-notifications:TAG
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet: { path: /health, port: 8000 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 8000 }
            initialDelaySeconds: 10
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: svc-notifications
spec:
  selector:
    app: svc-notifications
  ports:
    - port: 80
      targetPort: 8000
  type: ClusterIP
